export const CELL_SIZE = Number(import.meta.env.VITE_CELL_SIZE) || 20;

export {
  GRID_SIZE,
  POINTS_PER_FOOD,
  LEVEL_COUNT,
  INITIAL_SNAKE,
  DIRECTION_OPPOSITE,
  KEY_MAP,
  RUNNER_LANE_X,
  RUNNER_VIEWPORT_TAIL,
} from '../game';
