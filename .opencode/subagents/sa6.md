---
description: Subagent 6 - Code Review
---

1. Open your terminal/shell execution tool.
2. Execute the following command:
   `opencode run "Read and execute the instructions in .opencode/prompts/006CodeReview.md to review the completed implementation" --model "opencode/minimax-m3-free"`
3. Wait for the terminal process to finish and return a success exit code.
4. Once the command completes successfully, update the `LAST_SUBAGENT` key to `SA6` in the file `.opencode/current-state.md`.
