---
description: Subagent 5 - Implement
---

1. Open your terminal/shell execution tool.
2. Execute the following command:
   `opencode run "Read and execute the instructions in .opencode/prompts/005Implement.md to implement the approved plan" --model "opencode-go/qwen3.6-plus"`
3. Wait for the terminal process to finish and return a success exit code.
4. Once the command completes successfully, update the `LAST_SUBAGENT` key to `SA5` in the file `.opencode/current-state.md`.
