---
description: Subagent 3 - Update Plan
---

1. Open your terminal/shell execution tool.
2. Execute the following command:
   `opencode run "Read and execute the instructions in .opencode/prompts/003UpdatePlan.md to update the implementation plan from review findings" --model "opencode-go/deepseek-v4-pro"`
3. Wait for the terminal process to finish and return a success exit code.
4. Once the command completes successfully, update the `LAST_SUBAGENT` key to `SA3` in the file `.opencode/current-state.md`.
